package 연습장;

public class 컬렉션함수들 {

	/*
	 	add(); , add(index, element);
	 	get();
	 	size();
	 	remove(index); , remove(object);
	 	removeAll(Collection);
		contains(object);
		containsAll(Collection);
		addAll(Collection);	
		retainAll(Collection);
		
		LinkedList--------------------------------------------
			
			pop(), poll(), pollLast()
		
		TreeSet(Comparator 필요)--------------------------------
		
			ceiling(), floor(), higher(), lower()
			descendingSet() : 이미정렬해놓은 반대방향으로 바꿈
			headSet(toElement) : 맨앞부터 원한는 요소까지 자른 set반환
			tailSet(toElement) : 원하는 요소부터 맨뒤까지 set을 반환
			subSet(fromeElement, toElemnet)
			
		HashMap(key와 value가 한쌍을 이루는구조, key는 중복허용x)
		
			put(key, value)
			get(key) : key를 이용해 value를 꺼냄
			keySet() : key들로만 이루어진 Set을 반환한다
			values() : value들로만 이루어진 Set을 반환한다
			entrySet() : Entry<key, value>로 이루어진 set을 반환한다
			A instanceof B : A가 B의 인스턴스인지를 판별한다. 업캐스팅된 상태에서도 판별된다
			containskey(), containsvalue()
			
		Collections
		
		collections.addAll
	 */
}
